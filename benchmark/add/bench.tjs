WebAssembly
  .instantiate(wasmmodule)
  .then((m) => {
    showHeader();

    var t0 = now();

    for (let i = 0; i < NBINTERATION; i++) {
      m.exports.add(1, 1);
    }

    var t1 = now();
    console.log("took", t1 - t0);
  });
