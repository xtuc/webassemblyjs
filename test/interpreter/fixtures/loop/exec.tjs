let nbcalled = 0;

function fn() {
  nbcalled++;
}

const importObject = {
  module: {
    fn
  }
};

it('should call the function multiple times', () => {
  const m = WebAssembly.instantiateFromSource(watfmodule, importObject);

  m.exports.main();

  console.log(nbcalled);
  assert.notEqual(nbcalled, 0);
});
