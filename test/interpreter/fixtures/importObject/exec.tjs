function fn() {
  console.log("called");
}

const importObject = {
  module: {
    fn
  }
};

it('should import the function', () => {
  const m = WebAssembly.instantiateFromSource(watfmodule, importObject);

  console.log(m);
});
